# DATABASE_URL: connection string do Postgres (Neon, Supabase, etc.).
# Ainda não é exigida para rodar `npm run dev` — o app usa dados mock
# (src/data/initialData.ts) nesta etapa. Necessária só para `npm run db:migrate`.
DATABASE_URL="postgresql://usuario:senha@host/banco?sslmode=require"
